<%= form_with url: core_tenant_path(@tenant), method: :get, data: { turbo_frame: "tenant_tab" }, class: "flex items-center gap-3" do |f| %>
  <%= f.hidden_field :tab, value: "locks" %>
  <div class="flex items-center gap-2">
    <label class="text-sm text-gray-500 shrink-0">From</label>
    <input type="date" name="from" value="<%= @from %>"
           class="text-sm bg-white border border-gray-200 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900 focus:border-transparent">
  </div>
  <div class="flex items-center gap-2">
    <label class="text-sm text-gray-500 shrink-0">To</label>
    <input type="date" name="to" value="<%= @to %>"
           class="text-sm bg-white border border-gray-200 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900 focus:border-transparent">
  </div>
  <%= f.submit "Filter", class: "px-3 py-2 text-sm font-medium bg-gray-900 text-white rounded-lg hover:bg-gray-700 transition-colors cursor-pointer" %>
  <% if @from.present? || @to.present? %>
    <%= link_to "Clear", core_tenant_path(@tenant, tab: "locks"), class: "text-sm text-gray-400 hover:text-gray-900", data: { turbo_frame: "tenant_tab" } %>
  <% end %>
<% end %>

<div class="bg-white border border-gray-200 rounded-xl overflow-hidden">
  <% if @access_grants.any? %>
    <table class="w-full text-sm">
      <thead>
        <tr class="border-b border-gray-100 bg-gray-50">
          <th class="px-4 py-3 text-left font-medium text-gray-500">Unit</th>
          <th class="px-4 py-3 text-left font-medium text-gray-500 hidden sm:table-cell">Location</th>
          <th class="px-4 py-3 text-left font-medium text-gray-500 hidden sm:table-cell">From</th>
          <th class="px-4 py-3 text-left font-medium text-gray-500">Until</th>
          <th class="px-4 py-3 text-left font-medium text-gray-500">Status</th>
          <th class="px-4 py-3"></th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100">
        <% @access_grants.each do |grant| %>
          <% past = grant.revoked_at.present? || grant.ends_at <= Time.current %>
          <tr class="hover:bg-gray-50 transition-colors">
            <td class="px-4 py-3 font-medium text-gray-900">
              <%= link_to "Unit #{grant.lock.unit_identifier}", core_lock_path(grant.lock), class: "hover:underline", data: { turbo_frame: "_top" } %>
            </td>
            <td class="px-4 py-3 text-gray-500 hidden sm:table-cell"><%= grant.lock.location.name %></td>
            <td class="px-4 py-3 text-gray-500 hidden sm:table-cell"><%= grant.starts_at.strftime("%-d %b %Y") %></td>
            <td class="px-4 py-3 text-gray-500"><%= grant.ends_at.strftime("%-d %b %Y") %></td>
            <td class="px-4 py-3">
              <% if grant.revoked_at.present? || grant.ends_at <= Time.current %>
                <span class="text-xs text-gray-400">Past</span>
              <% elsif grant.starts_at > Time.current %>
                <span class="text-xs font-medium text-amber-600">Upcoming</span>
              <% else %>
                <span class="text-xs font-medium text-green-600">Active</span>
              <% end %>
            </td>
            <td class="px-4 py-3 text-right">
              <% unless past %>
                <%= link_to "Edit", edit_core_access_grant_path(grant, return_to: request.original_url),
                      class: "px-2.5 py-1 text-xs font-medium text-gray-500 border border-gray-200 rounded-lg hover:bg-gray-50 hover:text-gray-900 transition-colors",
                      data: { turbo_frame: "_top" } %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <div class="px-4 py-12 text-center text-sm text-gray-400">
      <% if @from.present? || @to.present? %>
        No access grants found for the selected dates.
      <% else %>
        No access grants yet.
      <% end %>
    </div>
  <% end %>
</div>
